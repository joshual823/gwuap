node_modules
.next
.env.local
.env*.local
.DS_Store
.vercel
.env*

# Generated by `tsc --noEmit` when incremental builds are on.
tsconfig.tsbuildinfo

# Written by `next dev` on every boot (Next.js 16). Build output, not
# instructions we wrote — committing them means a diff after every run.
AGENTS.md
CLAUDE.md
